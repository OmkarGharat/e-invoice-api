{
  "name": "e-invoice-api",
  "version": "1.0.0",
  "description": "E-Invoice Testing API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --config=jest.config.js --retryTimes=1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-html-reporter": "^4.3.0",
    "supertest": "^7.2.2"
  }
}